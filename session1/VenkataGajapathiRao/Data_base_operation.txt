--Crud Operaions 
/*
CREATE TABLE Students (
    student_id INT PRIMARY KEY,
    name VARCHAR(50),
    age INT,
    department VARCHAR(30)
);

--adding a column
alter table Students add village varchar(30);

--modifying a column
alter table Students modify village varchar(20);

alter table Students rename column village to village_name;

alter table Students drop Column village_name;


--Insertion of the data
--insert into Students values (1,"Shahir",22,"IT"),(2,"sd",123,"IIT");

--Updation of values 
update Students set name="Hello" where student_id=1;

INSERT INTO Students (student_id, name, age, department) VALUES
(2, 'Alice', 20, 'CSE'),
(3, 'Bob', 21, 'ECE'),
(4, 'Charlie', 23, 'IT'),
(5, 'David', 22, 'MECH'),
(6, 'Eva', 20, 'CSE'),
(7, 'Frank', 24, 'EEE'),
(8, 'Grace', 21, 'IT'),
(9, 'Henry', 22, 'CIVIL'),
(10, 'Ivy', 23, 'ECE'),
(11, 'Jack', 20, 'CSE');
*/

--select * from Students where age>22;
--select * from Students where name like 'A%';
--select * from Students where name like 'B%';

--select * from Students where department='CSE' or age>22;

--Group by
--select count(*) as number_of,department from Students group by department having count(*)>2;




--Sql Problems 
-- for findin the second max element 
--create table employe(id int PRIMARY KEY,
--                     name varchar(30));


--Insert into employe(id,name) values (1,100),(2,200),(3,300),(5,300);
--Insert into employe(id,name) values (5,300);

--select distinct(name) from employe order by name DESC limit 1 offset 1;


--Giving rankings 
/*
CREATE TABLE Scores (
    id INT PRIMARY KEY,
    score DECIMAL(4,2)
);

INSERT INTO Scores (id, score) VALUES
(1, 3.50),
(2, 3.65),
(3, 4.00),
(4, 3.85),
(5, 4.00),
(6, 3.65),
(7, 2.75),
(8, 3.20),
(9, 4.50),
(10, 4.50),
(11, 1.90),
(12, 3.85);


select score,Dense_rank() over(order by score DESC) from Scores;
*/

/*
-- Create Customers table
CREATE TABLE cust1 (
    id INTEGER PRIMARY KEY,
    name TEXT
);
*/
/*
-- Create Orders table
CREATE TABLE ord1 (
    id INTEGER PRIMARY KEY,
    customerId INTEGER,
    FOREIGN KEY (customerId) REFERENCES cust1(id)
);

-- Insert data into Customers
INSERT INTO cust1 (id, name) VALUES
(1, 'Joe'),
(2, 'Henry'),
(3, 'Sam'),
(4, 'Max'),
(5, 'Alice'),
(6, 'Bob'),
(7, 'Charlie');

-- Insert data into Orders
INSERT INTO ord1 (id, customerId) VALUES
(1, 3),   -- Sam
(2, 1),   -- Joe
(3, 5),   -- Alice
(4, 5),   -- Alice (second order)
(5, 7);   -- Charlie


select * from Cust1 where id in (select id from Cust1 except select id from Ord1);

*/




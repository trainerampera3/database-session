**Used Car Market Analytics**

Using Streamlit and connected to a database using psycopg for data retrieval from the database to create a dashboard which shows the used cars comparison, price analysis, and more.
We previously built the same dashboard, but the data retrieval was from a CSV file; 
however, we have now transitioned it to pull from the database by storing the data in a database.
